'use client';

export default function StorefrontSettingsPage() {
  return (
    <div className="flex items-center justify-center h-64 bg-surface-container-low rounded-xl border border-dashed border-outline-variant">
      <p className="text-on-surface-variant text-body-md">[ Storefront settings — design pending ]</p>
    </div>
  );
}
